name: tv_volume_widget
description: "A TV-friendly audio output device switcher and volume control widget for Windows."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  win32: ^5.12.0
  shared_preferences: ^2.3.0
  hotkey_manager: ^0.2.0
  tray_manager: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
